import React from "react";
import ReactLoading from "react-loading";

const Loading: React.FC = () => {
  return <ReactLoading type="spin" color="#fff" height={30} width={30} />;
};

export default Loading;
